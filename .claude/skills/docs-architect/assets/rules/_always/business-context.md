<!-- Always-loaded rule. Copy to .claude/rules/business-context.md (no subfolder). -->

# Business context

- **Product**: [1 sentence — what it is and who uses it]
- **Core value**: [1 sentence — why this exists, the problem it solves]
- **Ecosystem actors**: [actor 1, actor 2, actor 3] and their roles in 1 line each
- **Main user flow**: [step 1 → step 2 → step 3] — the single critical path the product delivers
- **Privacy / security constraints**: [e.g. "user data never leaves EU", "all writes require auth"]
- **Hard business rules**: [rules that cannot be violated, e.g. "one active subscription per user"]
- **Out of scope**: [what we explicitly do NOT do, to prevent scope creep in suggestions]
