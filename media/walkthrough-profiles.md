# Profiles tab

Type a name in the input and click **Save loadout** to snapshot your current active setup. Use **Apply** to switch between profiles in one click — or press `Cmd+Shift+P` → "Loadout: Apply Profile…".
